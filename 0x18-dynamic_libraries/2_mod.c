#include <stdio.h>

/**
 * mod - a function that finds the remainder of an integer
 * using another integer
 * @x: an integer
 * @y: an integer
 *
 * Return: 0
 */

int mod(int x, int y)
{
	int ans = x % y;

	return (ans);
}
